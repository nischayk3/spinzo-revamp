import { pgTable, uuid, varchar, timestamp, integer, doublePrecision, text, boolean, decimal, date, jsonb } from 'drizzle-orm/pg-core'; import { InferSelectModel, InferInsertModel } from 'drizzle-orm'; // Example table for users export const users = pgTable('users', { id: uuid('id').primaryKey().defaultRandom(), firebaseUid: varchar('firebase_uid', { length: 128 }).unique().notNull(), phone: varchar('phone', { length: 15 }).unique().notNull(), name: varchar('name', { length: 255 }), email: varchar('email', { length: 255 }), gender: varchar('gender', { length: 20 }), referralCode: varchar('referral_code', { length: 50 }), role: varchar('role', { length: 20 }).default('customer'), credits: integer('credits').default(0), subscriptionStatus: varchar('subscription_status', { length: 20 }).default('inactive'), createdAt: timestamp('created_at').defaultNow(), updatedAt: timestamp('updated_at').defaultNow(), }); export type User = InferSelectModel<typeof users>; export type NewUser = InferInsertModel<typeof users>;
